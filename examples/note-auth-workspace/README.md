# Note with authentication

The same Note CRUD API with method-level `@Auth()` on all five routes.

Read the [API guide](../../docs/AI_API_RULES.md) and
[porting guide](../../docs/PORTING.md). Select this workspace through
`APP_WORKSPACE` or Console > Settings, then restart the server.
